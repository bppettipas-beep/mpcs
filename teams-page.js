(()=>{const api='https://mpcsdiscord-production.up.railway.app/api/schedule',set=(id,value)=>{const element=document.getElementById(id);if(element)element.textContent=String(value)},status=document.getElementById('teamDataStatus');fetch(api,{cache:'no-store',headers:{Accept:'application/json'}}).then(response=>{if(!response.ok)throw new Error(`HTTP ${response.status}`);return response.json()}).then(data=>{const teams=(Array.isArray(data.teams)?data.teams:[]).filter(team=>team&&team.name),players=teams.reduce((total,team)=>total+(Array.isArray(team.players)?team.players.filter(player=>(typeof player==='string'?player:player?.name)).length:0),0);set('registeredTeamCount',teams.length);set('rosteredPlayerCount',players);if(status)status.textContent=`LIVE FIELD · ${teams.length} TEAM${teams.length===1?'':'S'} REGISTERED`}).catch(()=>{if(status)status.textContent='LIVE DATA TEMPORARILY UNAVAILABLE'})})();
